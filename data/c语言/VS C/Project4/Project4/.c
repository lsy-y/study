#include<stdio.h>
#include<math.h>
void main()
{
	double x, y, z; x = y = z = 0.5, 2.0, 5.75;
	printf("%f %f %f", x, y, z);
	int m, a; a = 1; m = ++a * 5;
	printf("%d", m);
	double y;
	y = sqrt(100);
	printf("%d", y);
}